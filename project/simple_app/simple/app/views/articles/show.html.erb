<h1> <%= @article.title %> </h1>

<h1> <%= @article.body %> </h1>

<ul>

<div>
    <%= link_to "Edit" ,edit_article_path(@article) %>|
    <%= link_to "Back" ,articles_path %>|
    <%= link_to "Destroy" , article_path(@article) ,  data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>

    <%= link_to "Hello" , article_url  ,data: {
                    turbo_method: :patch,
                    turbo_confirm: "Hello"
                  } %>
</div>

</ul>